# LecQ 개발 체크리스트 & 스킬가이드

## 📋 개발 전 준비물

### 개발 환경 세팅
- [ ] Node.js 18+ 설치
- [ ] npm/yarn 설치
- [ ] SQLite3 또는 PostgreSQL 설치
- [ ] VSCode 설치 및 필요 확장 설정
  - [ ] ES7+ React/Redux/React-Native snippets
  - [ ] Prettier (코드 포매팅)
  - [ ] SQLite (DB 브라우저)

### 프로젝트 초기화
- [ ] `npx create-react-app lecq-frontend` (프론트엔드)
- [ ] `npm init -y` && `npm install express` (백엔드)
- [ ] `.gitignore` 작성
- [ ] 폴더 구조 설계

---

## 🎯 프론트엔드 개발 체크리스트

### 1. 라이브러리 설치

```bash
# UI & 상태 관리
npm install axios react-router-dom

# PDF 렌더링
npm install react-pdf pdfjs-dist

# 아이콘 & UI 컴포넌트 (선택)
npm install lucide-react

# 유틸리티
npm install lodash debounce
```

- [ ] 모든 라이브러리 설치 완료
- [ ] package.json 확인

### 2. 폴더 구조

```
src/
├── components/          # 재사용 가능한 컴포넌트
│   ├── Layout/
│   ├── Auth/           # 로그인, 회원가입, 로그아웃
│   ├── DocumentList/   # 문서함
│   ├── PDFViewer/      # PDF 뷰어
│   ├── Tabs/           # 요약, 문제, 노트, AI튜터 탭
│   └── Common/
├── pages/               # 페이지 컴포넌트
│   ├── LandingPage.jsx
│   ├── RegisterPage.jsx
│   ├── LoginPage.jsx
│   ├── DocumentsPage.jsx
│   ├── StudyPage.jsx
│   └── SettingsPage.jsx
├── api/                 # API 호출 함수
│   ├── auth.js
│   ├── documents.js
│   ├── study.js
│   └── api.js (axios 인스턴스)
├── hooks/               # 커스텀 훅
│   ├── useAuth.js
│   ├── useDebounce.js
│   └── useAPI.js
├── context/             # React Context (상태 관리)
│   └── AuthContext.js
├── styles/              # 스타일시트
│   ├── global.css
│   └── variables.css
├── utils/               # 유틸리티 함수
│   ├── validation.js
│   └── storage.js
└── App.jsx
```

- [ ] 폴더 구조 생성 완료

### 3. 라우팅 설정 (App.jsx)

**필수 구현:**
```javascript
// App.jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { AuthProvider } from './context/AuthContext';
import PrivateRoute from './components/Auth/PrivateRoute'; // JWT 확인

export default function App() {
  return (
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          {/* 공개 라우트 */}
          <Route path="/" element={<LandingPage />} />
          <Route path="/register" element={<RegisterPage />} />
          <Route path="/login" element={<LoginPage />} />

          {/* 보호된 라우트 (JWT 필요) */}
          <Route element={<PrivateRoute />}>
            <Route path="/documents" element={<DocumentsPage />} />
            <Route path="/study/:pdfId" element={<StudyPage />} />
            <Route path="/settings" element={<SettingsPage />} />
          </Route>
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  );
}
```

- [ ] App.jsx 작성 완료
- [ ] 모든 페이지 경로 정의

### 4. 인증 구현 (AuthContext + API)

**필수 파일:**

#### `api/auth.js`
```javascript
import api from './api';

export const register = (email, password) =>
  api.post('/auth/register', { email, password });

export const login = (email, password) =>
  api.post('/auth/login', { email, password });

export const logout = () => {
  localStorage.removeItem('token');
};

export const deleteAccount = () =>
  api.delete('/auth/me');
```

#### `api/api.js`
```javascript
import axios from 'axios';

const api = axios.create({
  baseURL: 'http://localhost:4000/api',
});

// JWT를 모든 요청의 Authorization 헤더에 추가
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default api;
```

#### `context/AuthContext.js`
```javascript
import { createContext, useState, useEffect } from 'react';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);
  const [token, setToken] = useState(localStorage.getItem('token'));

  useEffect(() => {
    // 페이지 로드 시 localStorage에서 토큰 확인
    if (token) {
      // 필요시 토큰 검증 로직 추가
    }
    setLoading(false);
  }, [token]);

  return (
    <AuthContext.Provider value={{ user, token, setToken, loading }}>
      {children}
    </AuthContext.Provider>
  );
}
```

- [ ] api/auth.js 작성
- [ ] api/api.js (axios 인스턴스) 작성
- [ ] AuthContext 작성
- [ ] PrivateRoute 컴포넌트 작성

### 5. 페이지별 구현 체크리스트

#### 5-1. LandingPage.jsx (/)
- [ ] 서비스 소개 UI
- [ ] [회원가입], [로그인] 버튼
- [ ] 로그인 상태면 /documents로 redirect

#### 5-2. RegisterPage.jsx (/register)
- [ ] 이메일 입력 필드 + 정규식 검증
- [ ] 비밀번호 입력 필드 + 규칙 검증 (8자+, 영문+숫자)
- [ ] 비밀번호 확인 필드 + 일치 검증
- [ ] [가입하기] 버튼
- [ ] 오류 메시지 표시 (중복 이메일, 규칙 미충족 등)
- [ ] 성공 후 /login으로 이동

#### 5-3. LoginPage.jsx (/login)
- [ ] 이메일 입력 필드
- [ ] 비밀번호 입력 필드
- [ ] [로그인] 버튼
- [ ] '계정이 없으신가요? 회원가입' 링크
- [ ] 오류 메시지 표시
- [ ] 성공 후 JWT localStorage 저장 + /documents 이동

#### 5-4. DocumentsPage.jsx (/documents)
- [ ] 상단 네비게이션 바 (로고, [로그아웃], [설정])
- [ ] [PDF 업로드] FAB 버튼
  - [ ] multer 파일 선택 + 유효성 검증 (20MB, 50페이지)
  - [ ] POST /api/pdfs 호출 (multipart/form-data)
  - [ ] 업로드 진행 상황 표시
- [ ] PDF 카드 그리드 (파일명, 날짜, 요약/문제 상태)
- [ ] 각 카드에 [삭제] 버튼
- [ ] 카드 클릭 → /study/:pdfId 이동

#### 5-5. StudyPage.jsx (/study/:pdfId)
- [ ] 상단 네비게이션 (PDF 파일명, [로그아웃], [설정])
- [ ] 좌측 PDF 뷰어 (60%)
  - [ ] react-pdf로 PDF 렌더링
  - [ ] [←] [→] 페이지 컨트롤
  - [ ] 현재 페이지/전체 페이지 표시
- [ ] 우측 탭 패널 (40%)
  - [ ] [요약], [문제], [노트], [AI튜터] 탭
  - [ ] 기본 탭: 요약

#### 5-6. 탭 구현 상세

##### **요약 탭**
- [ ] GET /api/pdfs/:pdfId/summary 호출
- [ ] status별 UI:
  - [ ] processing: 스피너 + '요약을 생성하고 있습니다'
  - [ ] done: 한국어 요약 텍스트 (14px, line-height 1.8)
  - [ ] failed: 아이콘 + '요약 생성 실패' + [재시도] 버튼
- [ ] 3초마다 폴링 (processing 상태)
- [ ] [재시도] → POST /api/pdfs/:pdfId/summary/retry

##### **문제 탭**
- [ ] GET /api/pdfs/:pdfId/questions 호출
- [ ] status별 UI:
  - [ ] processing: 스피너
  - [ ] done: 문제 카드 5개 (문제 + 4선지)
  - [ ] failed: [재시도] 버튼
- [ ] 정답(answer) 미표시
- [ ] 클라이언트에서 answer 필드 제거

##### **노트 탭**
- [ ] GET /api/pdfs/:pdfId/note 호출
- [ ] textarea에 content 표시
- [ ] debounce(2000ms) 후 PUT /api/pdfs/:pdfId/note 자동 호출
- [ ] 저장 상태 표시 ('저장 중...' → '저장됨')

##### **AI 튜터 탭**
- [ ] POST /api/pdfs/:pdfId/chat 호출 (SSE)
- [ ] 대화 UI (메시지 좌우 정렬)
- [ ] textarea + [전송] 버튼
- [ ] Enter: 전송, Shift+Enter: 줄바꿈
- [ ] SSE 청크 수신 → 실시간 텍스트 표시
- [ ] 히스토리는 React state에만 유지 (DB 저장 X)
- [ ] 오류 처리: [재시도] 버튼

#### 5-7. SettingsPage.jsx (/settings)
- [ ] [탈퇴하기] 버튼
- [ ] 확인 다이얼로그
- [ ] DELETE /api/auth/me 호출
- [ ] 성공 후 JWT 삭제 + /login 이동

### 6. 유효성 검증 (utils/validation.js)

```javascript
export const validateEmail = (email) => {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return regex.test(email);
};

export const validatePassword = (password) => {
  // 8자+, 영문+숫자 포함
  return password.length >= 8 && /[a-zA-Z]/.test(password) && /[0-9]/.test(password);
};

export const validatePasswordMatch = (pwd1, pwd2) => pwd1 === pwd2;
```

- [ ] validation.js 작성 완료
- [ ] 모든 입력 필드에 적용

### 7. 스타일링 (CSS)

**설계 원칙:**
- Primary Color: #2C5F9E (팀 활성)
- Neutral: #F5F5F5 (배경), #E0E0E0 (보더)
- 타이포그래피: 본문 14px, 탭 13px

- [ ] global.css 작성
- [ ] variables.css (색상, 폰트 변수) 작성
- [ ] 각 컴포넌트 CSS 작성
- [ ] 반응형 디자인 (1280px 이상 기준)

---

## 🔧 백엔드 개발 체크리스트

### 1. 라이브러리 설치

```bash
npm install express
npm install cors
npm install dotenv
npm install multer              # 파일 업로드
npm install pdf-parse           # PDF 텍스트 추출
npm install @anthropic-ai/sdk   # Claude API
npm install bcryptjs            # 비밀번호 해싱
npm install jsonwebtoken        # JWT
npm install sqlite3             # 또는 pg (PostgreSQL)
npm install nodemon -D          # 개발용
```

- [ ] 모든 라이브러리 설치 완료

### 2. 폴더 구조

```
backend/
├── routes/
│   ├── auth.js           # 회원가입, 로그인, 탈퇴
│   ├── pdfs.js           # PDF 업로드, 조회, 삭제
│   ├── study.js          # 요약, 문제, 노트, AI튜터
│   └── index.js          # 라우트 통합
├── controllers/
│   ├── authController.js
│   ├── pdfController.js
│   └── studyController.js
├── models/
│   ├── User.js
│   ├── PDF.js
│   ├── Summary.js
│   ├── Questions.js
│   └── Note.js
├── middleware/
│   ├── auth.js           # JWT 검증
│   └── errorHandler.js
├── services/
│   ├── claudeService.js  # Claude API 호출
│   └── pdfService.js     # pdf-parse 래퍼
├── db/
│   ├── init.js           # DB 초기화 & 테이블 생성
│   └── connection.js     # DB 연결
├── uploads/              # 업로드된 PDF 저장소
├── .env                  # 환경변수
├── server.js             # 메인 서버 파일
└── package.json
```

- [ ] 폴더 구조 생성 완료

### 3. 환경변수 설정 (.env)

```
PORT=4000
NODE_ENV=development

# Database
DB_PATH=./db/lecq.db     # SQLite의 경우
# 또는 PostgreSQL
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=lecq
# DB_USER=postgres
# DB_PASSWORD=password

# JWT
JWT_SECRET=your_super_secret_key_change_in_production
JWT_EXPIRY=24h

# Claude API
ANTHROPIC_API_KEY=your_claude_api_key

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=20971520  # 20MB in bytes
```

- [ ] .env 파일 작성 (중요: .gitignore에 추가!)

### 4. DB 초기화 (db/init.js)

```javascript
// 5개 테이블 생성:
// 1. users
// 2. pdfs
// 3. summaries
// 4. questions
// 5. notes
```

**필수 컬럼:**

#### users
- userId (PK, INTEGER, AUTO INCREMENT)
- email (VARCHAR(255), UNIQUE, NOT NULL)
- passwordHash (VARCHAR(255), NOT NULL)
- createdAt (DATETIME, DEFAULT NOW())

#### pdfs
- pdfId (PK)
- userId (FK)
- originalName, storedName
- extractedText (TEXT, NULL)
- pageCount
- createdAt

#### summaries
- summaryId (PK)
- pdfId (FK, UNIQUE)
- content (TEXT, NULL)
- status (VARCHAR(20), DEFAULT 'processing')
- createdAt

#### questions
- questionId (PK)
- pdfId (FK, UNIQUE)
- content (TEXT, NULL - JSON 형식)
- status (VARCHAR(20), DEFAULT 'processing')
- createdAt

#### notes
- noteId (PK)
- pdfId (FK, UNIQUE)
- content (TEXT, DEFAULT '')
- updatedAt

- [ ] db/init.js 작성 완료
- [ ] DB 테이블 생성 확인

### 5. 미들웨어 구현

#### middleware/auth.js
```javascript
// JWT 검증
// req.headers.Authorization에서 토큰 추출 → 검증 → req.userId 설정
```

#### middleware/errorHandler.js
```javascript
// 에러 응답 표준화
// { "error": "메시지" }
```

- [ ] auth.js 작성
- [ ] errorHandler.js 작성

### 6. API 엔드포인트 구현

#### 6-1. 인증 (routes/auth.js)

- [ ] **POST /api/auth/register**
  - 입력: { email, password }
  - 검증: 이메일 정규식, 비밀번호 규칙
  - 처리: bcrypt 해싱 → DB 저장
  - 응답: { userId, email }
  - 오류: 409 (중복 이메일), 400 (검증 실패)

- [ ] **POST /api/auth/login**
  - 입력: { email, password }
  - 검증: bcrypt.compare
  - 처리: JWT 생성
  - 응답: { token }
  - 오류: 401 (미존재 또는 비밀번호 불일치)

- [ ] **DELETE /api/auth/me**
  - 인증 필요
  - 처리: ON DELETE CASCADE로 모든 데이터 삭제
  - 파일 삭제: /uploads/{userId}/

#### 6-2. PDF 관리 (routes/pdfs.js)

- [ ] **POST /api/pdfs**
  - multer 파일 업로드
  - pdf-parse 텍스트 추출
  - Promise.all로 요약/문제/노트 병렬 생성
  - 응답: { pdfId, originalName, summaryStatus, questionsStatus }

- [ ] **GET /api/pdfs**
  - 사용자의 모든 PDF 목록 (최신순)
  - 응답: [ { pdfId, originalName, createdAt, summaryStatus, questionsStatus } ]

- [ ] **DELETE /api/pdfs/:pdfId**
  - CASCADE 삭제
  - 파일 삭제

- [ ] **GET /api/pdfs/:pdfId/file**
  - PDF 파일 반환 (Content-Type: application/pdf)

#### 6-3. 학습 콘텐츠 (routes/study.js)

- [ ] **GET /api/pdfs/:pdfId/summary**
  - 응답: { status, content }

- [ ] **POST /api/pdfs/:pdfId/summary/retry**
  - Claude API 재호출
  - status='processing' 재설정

- [ ] **GET /api/pdfs/:pdfId/questions**
  - 응답: { status, content: [{question, options, answer}] }
  - 클라이언트에 answer 필드 제거 처리

- [ ] **POST /api/pdfs/:pdfId/questions/retry**

- [ ] **GET /api/pdfs/:pdfId/note**
  - 응답: { content, updatedAt }

- [ ] **PUT /api/pdfs/:pdfId/note**
  - 입력: { content }
  - debounce 처리 (클라이언트)

- [ ] **POST /api/pdfs/:pdfId/chat** (SSE)
  - 입력: { message, history }
  - extracted_text 기반 프롬프트
  - Claude API stream: true
  - SSE로 청크 전송

### 7. Claude API 통합 (services/claudeService.js)

```javascript
// 3가지 사용 사례:
// 1. 요약 생성: extractedText → 한국어 요약
// 2. 문제 생성: extractedText → 4선지 JSON 문제 5개
// 3. AI 튜터: extractedText + 질문 → 응답 (stream)
```

- [ ] claudeService.js 작성
  - [ ] 요약 생성 함수
  - [ ] 문제 생성 함수
  - [ ] AI 튜터 스트리밍 함수

### 8. PDF 텍스트 추출 (services/pdfService.js)

```javascript
// pdf-parse 래퍼
// 파일 경로 → extracted_text 반환
```

- [ ] pdfService.js 작성

### 9. server.js 작성

```javascript
// Express 앱 초기화
// CORS 설정
// 미들웨어 등록
// 라우트 연결
// 에러 핸들링
// 서버 시작
```

- [ ] server.js 작성 완료
- [ ] nodemon 설정 확인 (package.json scripts)

### 10. 테스트

```bash
# 서버 시작
npm run dev

# Postman/Insomnia에서 API 테스트
# 또는 curl 명령어로 테스트
```

- [ ] 모든 API 엔드포인트 테스트 완료
- [ ] 오류 처리 확인
- [ ] 데이터베이스 저장 확인

---

## ✅ 통합 테스트 & 최종 점검

### 전체 플로우 테스트

- [ ] **회원가입 → 로그인 → PDF 업로드 → 학습**
  1. 회원가입 성공
  2. 로그인 성공 (JWT 발급)
  3. PDF 업로드 (요약/문제 자동 생성)
  4. 요약 탭 조회 (status 폴링)
  5. 문제 탭 조회
  6. 노트 탭 수정 (debounce 저장)
  7. AI 튜터 챗 (SSE)

- [ ] **문서함 조회 & 삭제**
  1. /documents 접속 (모든 PDF 목록)
  2. PDF 카드 클릭 → /study/:pdfId
  3. 삭제 버튼 → CASCADE 삭제

- [ ] **회원 탈퇴**
  1. /settings 접속
  2. [탈퇴하기] → 모든 데이터 삭제

### 경계값 테스트

- [ ] 파일 크기: 20MB 이상 업로드 시 오류
- [ ] 페이지 수: 50페이지 초과 업로드 시 오류
- [ ] JWT 만료: 24시간 후 401 반환
- [ ] 다른 사용자 리소스: 403 Forbidden

### 성능 테스트

- [ ] 요약 생성 시간 < 30초
- [ ] AI 튜터 첫 응답 < 2초
- [ ] 노트 저장 debounce 동작 확인

---

## 🐛 버그 방지 체크리스트

- [ ] **보안**
  - [ ] 비밀번호는 평문 저장 금지 (bcrypt 필수)
  - [ ] JWT 토큰 검증 (매 요청)
  - [ ] CORS 설정 (클라이언트 도메인 화이트리스트)
  - [ ] .env 파일 .gitignore 등록

- [ ] **데이터 무결성**
  - [ ] ON DELETE CASCADE 설정 (부모 삭제 시 자식 자동 삭제)
  - [ ] UNIQUE 제약 (email, pdfId FK 조합)
  - [ ] 이메일 정규식 검증

- [ ] **API 응답 일관성**
  - [ ] 성공: { "data": { ... } }
  - [ ] 오류: { "error": "메시지" }
  - [ ] HTTP 상태 코드 정확성

- [ ] **파일 처리**
  - [ ] 업로드 파일 UUID 변환 (경로 공격 방지)
  - [ ] 파일 삭제 시 서버 파일도 삭제
  - [ ] 파일 크기 제한 (클라이언트 + 서버)

- [ ] **사용자 경험**
  - [ ] 로딩 스피너 표시 (요약/문제 생성 중)
  - [ ] 오류 메시지 명확함 (사용자 입장)
  - [ ] 저장 상태 표시 (노트 debounce)

---

## 📝 개발 순서 (권장)

### Phase 1: 기초 (1주)
1. 프론트엔드 폴더 구조 & 라우팅
2. 백엔드 폴더 구조 & DB 초기화
3. 인증 API (회원가입, 로그인)
4. 인증 UI (RegisterPage, LoginPage)

### Phase 2: 핵심 기능 (1주)
5. PDF 업로드 API & UI
6. 요약/문제 자동 생성 (Claude API)
7. 학습 탭 UI (요약, 문제, 노트)

### Phase 3: 고급 기능 (1주)
8. AI 튜터 (SSE 스트리밍)
9. 노트 debounce 저장
10. 문서함 조회 & 삭제

### Phase 4: 마무리 (1주)
11. 유효성 검증 & 오류 처리
12. 전체 통합 테스트
13. UI/UX 개선 (스타일, 반응형)
14. 배포 준비

---

## 🚀 배포 체크리스트

- [ ] 프로덕션 환경변수 설정
- [ ] React 빌드: `npm run build`
- [ ] 정적 파일 서빙 설정 (Express static)
- [ ] 데이터베이스 마이그레이션 (SQLite → PostgreSQL)
- [ ] CORS 도메인 설정
- [ ] SSL/TLS 인증서 (HTTPS)
- [ ] 로깅 및 모니터링 설정

---

**이 체크리스트를 따라 개발하면 명세서와 100% 일치하는 프로젝트가 완성됩니다!**
