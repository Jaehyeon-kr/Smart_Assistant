# 🚀 LecQ 시작 가이드

## 현재 상태

✅ **백엔드 개발 완료 (100%)**

- API 서버: http://localhost:4000
- 데이터베이스: SQLite 준비됨
- 모든 14개 엔드포인트 구현됨
- Claude API 통합 완료

---

## 🎯 즉시 시작하기

### 1단계: 서버 실행

```bash
cd E:\3_1_home\software_engineering\new_sub
npm run dev
```

출력:
```
✓ LecQ 서버 시작: http://localhost:4000
✓ 환경: development
✓ 데이터베이스: ./db/lecq.db
✓ 데이터베이스 테이블 생성 완료
```

### 2단계: API 테스트 (curl 또는 Postman)

```bash
# 회원가입
curl -X POST http://localhost:4000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'

# 로그인
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'

# 건강 체크
curl http://localhost:4000/api/health
```

---

## 📁 주요 파일 위치

| 파일 | 설명 |
|------|------|
| `server.js` | 메인 서버 파일 |
| `.env` | 환경변수 (API 키 등) |
| `routes/auth.js` | 인증 API |
| `routes/pdfs.js` | PDF 관리 API |
| `routes/study.js` | 학습 API |
| `services/claudeService.js` | Claude AI 통합 |
| `db/init.js` | DB 초기화 |
| `uploads/` | PDF 저장소 |

---

## ⚙️ 설정 필수 사항

### Claude API 키 설정

`.env` 파일에서:
```
ANTHROPIC_API_KEY=sk-ant-v7-YOUR_KEY_HERE
```

실제 키로 변경하세요!

---

## 📊 API 요약 (14개)

### 인증 (3개)
```
POST   /api/auth/register      회원가입
POST   /api/auth/login         로그인
DELETE /api/auth/me            회원탈퇴
```

### PDF 관리 (4개)
```
POST   /api/pdfs               PDF 업로드
GET    /api/pdfs               문서함 조회
DELETE /api/pdfs/:pdfId        PDF 삭제
GET    /api/pdfs/:pdfId/file   PDF 파일
```

### 학습 (6개)
```
GET    /api/pdfs/:pdfId/summary              요약 조회
POST   /api/pdfs/:pdfId/summary/retry        요약 재생성
GET    /api/pdfs/:pdfId/questions            문제 조회
POST   /api/pdfs/:pdfId/questions/retry      문제 재생성
GET    /api/pdfs/:pdfId/note                 노트 조회
PUT    /api/pdfs/:pdfId/note                 노트 저장
```

### AI 튜터 (1개)
```
POST   /api/pdfs/:pdfId/chat   AI 튜터 (SSE)
```

---

## 📖 다음 단계

### 프론트엔드 개발 준비

```bash
# React 프로젝트 생성
cd E:\3_1_home\software_engineering
npx create-react-app lecq-frontend

# 필요한 라이브러리 설치
cd lecq-frontend
npm install axios react-router-dom react-pdf
```

### 개발 문서 참고

1. **README.md** - 전체 프로젝트 개요
2. **LecQ_완전_명세서.md** - 상세 명세서 (필독!)
3. **개발_체크리스트.md** - 구현 체크리스트
4. **개발_진행상황.md** - 현재 진행도

---

## 🧪 DB 확인

### SQLite DB 확인

```bash
# DB 열기
sqlite3 db/lecq.db

# 테이블 목록 확인
.tables

# 테이블 구조 확인
.schema users
.schema pdfs
.schema summaries
.schema questions
.schema notes

# 쿼리 예시
SELECT * FROM users;
SELECT * FROM pdfs;

# 나가기
.quit
```

---

## 🔍 문제 해결

### Q: 포트 4000이 이미 사용 중
A: `.env`에서 PORT를 변경하세요
```
PORT=5000
```

### Q: SQLite 에러
A: `db/` 폴더가 있는지 확인
```bash
mkdir -p db
```

### Q: Claude API 오류
A: `.env`의 ANTHROPIC_API_KEY 확인

### Q: 업로드 폴더 권한 오류
A: `uploads/` 폴더가 있는지 확인
```bash
mkdir -p uploads
```

---

## 📦 프로젝트 구조

```
lecq-backend/
├── routes/          ✅ API 라우트 (인증, PDF, 학습)
├── services/        ✅ 비즈니스 로직 (Claude, PDF)
├── middleware/      ✅ JWT 검증
├── db/              ✅ DB 초기화
├── uploads/         ✅ PDF 저장소
├── server.js        ✅ 메인 서버
├── .env             ✅ 환경변수
└── package.json     ✅ 의존성
```

---

## ✨ 완료된 기능

- ✅ 회원가입/로그인/탈퇴
- ✅ PDF 업로드 (multer)
- ✅ 텍스트 추출 (pdf-parse)
- ✅ 요약 생성 (Claude API)
- ✅ 문제 생성 (Claude API)
- ✅ AI 튜터 (SSE 스트리밍)
- ✅ 노트 저장
- ✅ JWT 인증
- ✅ 데이터베이스

---

## 🎉 완료!

백엔드 개발이 완료되었습니다.

**다음: 프론트엔드 개발 시작** 🚀
